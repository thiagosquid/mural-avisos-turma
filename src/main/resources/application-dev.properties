spring.devtools.restart.pollInterval=4000

## PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/muralads?currentSchema=mural
spring.datasource.username=postgres
spring.datasource.password=postgres

spring.jpa.generate-ddl=true
spring.jpa.hibernate.ddl-auto=create
spring.jpa.properties.hibernate.default_schema=mural

#drop n create table again, good for testing, comment this in production
#spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

api.key=${TOKEN_PASSWORD_MURAL}
